class Triangle {
    constructor (base, height) {
        this.base = base;
        this.height = height;
    }
    area () {
        return 0.5 * this.base * this.height;
    }
}

const triangle1 = new Triangle (6, 4);
console.log(triangle1.area());
